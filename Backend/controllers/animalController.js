/*const vaccineSchedule = require("../utils/vaccineSchedule");

function generateSchedule(startDate) {
  return vaccineSchedule.map(item => ({
    ...item,
    dueDate: new Date(startDate.getTime() + item.day * 24 * 60 * 60 * 1000)
  }));
}

module.exports = {
  generateSchedule
};*/